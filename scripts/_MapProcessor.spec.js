/**[@test({ "title": "TruJS.simpleViewSystem._MapProcessor: " })]*/
function testMapProcessor(arrange, act, assert, module) {
    var mapProcessor;

    arrange(function () {
        mapProcessor = module(["TruJS.simpleViewSystem._MapProcessor", []]);


    });

    act(function () {

    });

    assert(function (test) {

    });
}